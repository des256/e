use crate::*;

fn compile_vertex_shader(module: &sr::Module,vertex_ident: &str,vertex_fields: Vec<(&str,sr::Type)>) -> Option<Vec<u8>> {
    Some(Vec::new())
}

fn compile_fragment_shader(module: &sr::Module) -> Option<Vec<u8>> {
    Some(Vec::new())
}
